import { StereoPanPipe } from './stereo-pan.pipe';

describe('StereoPanPipe', () => {
  it('create an instance', () => {
    const pipe = new StereoPanPipe();
    expect(pipe).toBeTruthy();
  });
});
