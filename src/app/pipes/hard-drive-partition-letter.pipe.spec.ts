import { HardDrivePartitionLetterPipe } from './hard-drive-partition-letter.pipe';

describe('HardDrivePartitionLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new HardDrivePartitionLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
